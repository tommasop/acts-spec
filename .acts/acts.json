{
  "manifest_version": "2.0.0",
  "quality_gate": {
    "test": "npm test && cd acts-core && zig build test",
    "lint": "node --check .opencode/plugins/acts.js && node --check .opencode/plugins/cbm.js",
    "typecheck": null,
    "build": "cd acts-core && zig build"
  },
  "hilt": {
    "auto_land_low": true
  }
}
