# Session note (1788437772)

Implemented ponytail-rules.js runtime plugin: hooks experimental.chat.system.transform, resolves current project AGENTS.md (CLAUDE.md fallback) per run, appends binding precedence directive + path; idempotent; PONYTAIL_DEFAULT_MODE=off honored. Wired into ponytail.zig (embedded via @embedFile, installFiles/installFilesGlobal, ensurePluginEntry refactor, probe_rels, 3 new tests) and cmdPonytailInstall (always ensures plugin, upgrade-safe, offline-safe). Docs updated (INTEGRATION.md, acts-ponytail.md). Local install updated: project .opencode/plugins/ponytail-rules.js + opencode.json, global ~/.config/opencode/plugin/ponytail-rules.js + global opencode.json; acts binary rebuilt to 2.6.1 with embedded plugin. Verified end-to-end with node harness (directive + idempotency + off switch + no-rules project).
