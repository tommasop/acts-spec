# Session note (1786016251)

Removed cbm.js plugin: CBM now a native MCP server (mcp.codebase-memory-mcp wired by acts setup). Fleet helpers moved to Zig: acts graph repos/index --all/bootstrap/span, acts tech-lead <id>, acts doc-risk <file>. New shared client cbm.zig + graph.zig + docrisk.zig. Docs/AGENTS/CI updated. Version 2.1.6.
